import {Component} from "@angular/core";

@Component({
    selector: "playlist",
    moduleId: module.id,
    templateUrl: "playlist.html"
})
export class PlaylistComponent {
    constructor() {
    }
}