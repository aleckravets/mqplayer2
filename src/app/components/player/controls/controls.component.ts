import {Component} from "@angular/core";

@Component({
    selector: "controls",
    moduleId: module.id,
    templateUrl: "controls.html"
})
export class ControlsComponent {
    constructor() {
    }
}